Project-local Codex subagents for this repository.

Installed from:
- https://github.com/VoltAgent/awesome-codex-subagents

Selected for this workspace:
- `nextjs-developer` for the `bib` Next.js app
- `react-specialist` for shared React component work
- `mobile-developer` for the Expo/React Native app in `mobile`
- `ui-fixer` for narrow UI bug patches
- `reviewer` for read-only PR-style review

Project-local agents in `.codex/agents/` should override any same-named global agents in `~/.codex/agents/`.
