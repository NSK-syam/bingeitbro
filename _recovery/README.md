Local preservation snapshot created on 2026-04-08 before machine cleanup.

Included here:
- Files copied from side worktrees that were not present in the main working tree
- Draft HTML, images, memes, scripts, and video assets found only in local worktrees

Not included in Git for safety or because they are generated:
- `mobile/.env.local`
- `bib/.env.local`
- build caches such as `.next/`, `.expo/`, `.open-next/`, and `node_modules/`
- local IDE metadata and worktree internals

If you need secrets from local `.env.local` files after deleting this machine, back them up separately before cleanup.
